# Replit config

run = "gunicorn app:app --bind 0.0.0.0:5000 --workers 1 --timeout 30"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -r requirements.txt && gunicorn app:app --bind 0.0.0.0:5000 --workers 1"]
